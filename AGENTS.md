# AGENTS.md — start here (for every AI agent)

This project is worked on by **several AI agents taking turns** (Claude, Codex/GPT, …).
You have **no memory of earlier sessions**. Everything you need is in this repo.

## 1. Before you do anything
1. Read **`STATUS.md`**: what is done, what is in progress, and the **next task**.
2. Read the last 3 entries of **`CHANGELOG.md`**: what the previous agent just did.
3. Skim **`super-casual/kit/registry.json`** and one finished component (Button is the template).

## 2. The project
A **shadcn-style mobile game UI kit** ("Super Casual") for the **Pixelfork AI Engine**, which generates hyper-casual and casual **2D/3D mobile web games** (canvas engines: Three.js, Babylon, Phaser, PixiJS).
The UI is **plain HTML + CSS** floating above the game canvas, plus one small runtime script (`sc.js`).
Other AI agents will later *use* this kit to build game screens, so every component must be self-documenting.

The owner is **non-technical**: explain results in short, plain language. **Work in small steps, one task at a time.** **End every reply with exactly one "Next task: …"** line.

## 3. Where things are
```
super-casual/kit/                ← THE LIBRARY (work here)
  core.css                       tokens, color palette, outlined 3D text, glossy surface
  sc.js                          runtime: builds components from attributes, icons, animations, JS API (window.SC)
  registry.json                  machine-readable list of every component (for AI agents)
  assets/                        icons (PNG) + offsets.css (optical centering, generated)
  components/<name>/<name>.css   component styles
  components/<name>/<name>.md    AI manual: install, options, examples, rules for AI agents
  docs/<name>.html               docs page (uses docs/docs.css + docs/docs.js → SCDocs.page({...}))
  examples/                      components used over a game canvas
super-casual/old-demos/          OLD demo pages (pre-kit). Reference only; screens will be rebuilt as Blocks.
tools/crop_sheet.py              cut an icon sprite sheet into PNGs
tools/icon_offsets.py            regenerate assets/offsets.css after adding icons
tools/serve.py                   local no-cache preview server
icons/                           raw icon sheets generated by the owner
_archive/                        LOCAL ONLY (gitignored): early experiments + reference screenshots. Never commit.
```

## 4. How to add or convert a component (follow exactly)
Copy the pattern of an existing component (e.g. `progress`, `timer`):
1. `components/<name>/<name>.css`: header comment listing every attribute. Use `class="sc-<name>"` + `data-*` attributes.
2. Behavior goes in **`sc.js`**: add a selector constant, an `upgrade<Name>(el)` builder, hook it into `upgradeOne` and the MutationObserver, and add observed attributes. Expose JS helpers on `window.SC`. Bump `version`.
3. `components/<name>/<name>.md`: AI manual (Install, Usage, Options table, Examples, JavaScript, **Rules for AI agents**).
4. `registry.json`: add an item (name, description, whenToUse, files, docs, dependencies, html, props, events, js, rules, examples). Bump `version`.
5. `docs/<name>.html` via `SCDocs.page({...})`, and add it to the `NAV` list in `docs/docs.js`.
6. **Test in a browser** (`python3 tools/serve.py 8765`, open `http://localhost:8765/super-casual/kit/docs/<name>.html`): every example renders, images load, live demos work. Check the phone size 375×812.
   Add automatic checks in `tools/tests/<name>.html` (copy an existing one), register it in `PAGES` in `tools/tests/index.html`, and open `http://localhost:8765/tools/tests/index.html`. Every page must PASS before you publish.
7. Update **`STATUS.md`** and **`CHANGELOG.md`** (see §7).

## 5. Design rules (the owner approved these; never break them)
- **Look:** chunky, glossy, candy colors, thick dark outline (`--sc-outline`), light top band + darker 3D bottom lip.
- **Text on buttons and UI:** white, thin outline + **slightly thicker bottom edge** (the `.sc-text` two-layer technique in `core.css`). Never use multi-direction `text-shadow` outlines.
- **Font:** one font per game. The kit uses **M PLUS Rounded 1c** (weight 900).
- **Outline thickness** comes only from `--sc-line`, `--sc-line-sm`, `--sc-text-stroke`, `--sc-text-drop`.
- **No sharp corners.** Every free corner is slightly rounded. Corners where a shape attaches to another element (e.g. a tag hanging from a header) stay sharp. Never use `clip-path: polygon()`.
- **Never draw image assets with code** (icons, items, characters, badges with pictures). Put a grey placeholder, write an image-generation prompt for the owner, then crop the sheet they return (`tools/crop_sheet.py`) and run `tools/icon_offsets.py`.
- **Icons are chosen by name** (`data-icon="coin"`). Only use names listed in `registry.json → icons`.
- **Round glossy buttons** use a full circular face (no bottom lip) so they never look like ovals.
- **Component default colors** must use `:where(...)` so `data-color` can override them.
- **Never use `text-overflow: ellipsis` on outlined `.sc-text`** (its outline copy isn't cut and leaves a black smudge). Clip and fade instead (`.sc-long` pattern).
- **Never reuse a CSS variable inside its own definition** (`--fs:var(--fs)` is invalid). Use a new name (`--fs:var(--cb-fs)`).
- **Mobile first:** every full-page UI is one `.sc-screen` (Screen Shell, no custom resize code); full-page screens never scroll, edge margins are real pixels (12px), and taps must give press feedback on iOS (`sc.js` handles it).
- Reference screenshots from other games (`_archive/references/`) are **inspiration only**. Never copy their exact art, and never commit them.

## 6. Git workflow
- Work on `main` in small commits. Don't rewrite history.
- Never commit reference images from other games (see `.gitignore`).
- The repo is **private**; GitHub Pages is off. Preview with `tools/serve.py`.

## 7. Handoff protocol (REQUIRED after every task)
When you finish a task (or are about to run out of budget), **before stopping**:

1. **`STATUS.md`**
   - Move the task to **Done** (with its version).
   - Put anything unfinished under **In progress** with exact notes: which files, what works, what is broken, what's next.
   - Set **Next task** to the single next step.
   - Update the **Last updated** line.
2. **`CHANGELOG.md`**: add an entry at the top, using the format in §8.
3. **Commit and publish** using the version name from §8.

If you stop mid-task without finishing, still commit with `WIP` in the message and describe the exact state under **In progress** so the next agent can continue.

## 8. Version names — agent letter is REQUIRED
Every published update gets a version: **`vMAJOR.MINOR.PATCH-<LETTER>`**

| Letter | Agent |
|--------|-------|
| **A**  | Claude (Anthropic) |
| **B**  | Codex / GPT (OpenAI) |

Take a new letter for any other agent (C, D, …) and add it to this table.

- **MINOR +1** for a new or converted component or block. **PATCH +1** for fixes and small changes.
- The number keeps counting across agents: if the last version was `v0.11.0-A` and Codex fixes a bug, it publishes `v0.11.1-B`.
- Keep `registry.json → version` and `sc.js → SC.version` equal to the number (without the letter).

**Publish steps:**
```bash
git add -A
git commit -m "v0.11.0-A: Count Bubble component"
git tag v0.11.0-A
git push origin main --tags
gh release create v0.11.0-A --title "v0.11.0-A — Count Bubble component" --notes "Short plain-language description of what changed"
```

**CHANGELOG entry format:**
```markdown
## v0.11.0-A — Count Bubble component
- Agent: A (Claude) · Date: 2026-09-15
- Done: what was built/fixed, in plain language
- Tested: what was checked and how
- Notes for next agent: anything surprising, open issues
```
